vagrant-cachesshconfig
====================

To install, simply run:

    vagrant plugin install vagrant-cachesshconfig

Everything else should start happening automagically™.

Synopsis
========



